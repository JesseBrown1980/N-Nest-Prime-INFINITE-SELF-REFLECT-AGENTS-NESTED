# Path 2 / DBWH / N-Nest inverse-gate relation — 2026-07-11

## One theorem in three forms

N-Nest, Path 2, and DBBH→DBWH all enforce the same shape:

```text
proposal or projection
  -> independent reconstruction/recomputation
  -> compare against the originating signature
  -> authorize only on equality
```

### N-Nest

```text
child.reported == watcher.recomputed_truth
```

### Path 2

```text
selected CRT shadows jointly identify one bounded source object
```

### DBBH→DBWH

```text
P(R(P(X))) = P(X)
```

The white side reconstructs a candidate and re-projects it; SHA, complete cylinder shadows, and
frequency shells must match the black projection before emission.

## Why the relation is substantive

A decoder returning a candidate is not sufficient. A recursive agent reporting an answer is not
sufficient. Both require a separately derived consistency surface.

```text
candidate exists                     -> not proof
candidate reproduces originating map -> proof gate
```

N-Nest applies this per node and ANDs all child subtrees upward. DBWH applies it per recovered slice.
The same depth-independent law prevents a fabricated signal from crossing into authority.

## Storage-backed recursion

N-Nest identities are generative from compact seeds. The full body need not remain resident:

```text
seed/PID/path           -> compact address state
active node + watcher   -> bounded RAM
reported/recomputed row -> HBP receipt
cold body/history       -> HDD/SSD
```

Combined with Path 1 and Path 2:

- Path 1 recalls a retained body from an authenticated address;
- Path 2 reconstructs a body from jointly sufficient distributed shadows;
- N-Nest verifies every recursive use before authority propagates.

This lets storage-rich, low-GPU machines act as recursive verifier and memory nodes. GPU inference
may still be called as an optional sidecar; disk does not perform neural matrix multiplication.

## Pre-Asolaria GNN relation

The neural proposal layer traces to Jesse's healthcare edge-level models and later BigPickle/Asolaria
GNN ensemble. GNNs score edges and paths; N-Nest and DBWH provide independent deterministic
verification. Neural inference is proposal/evidence, not final proof.

## Independent verification

### Claude Fable 5 — operator-supplied third-seat runs

```text
dbbh-coms-quant-prism       rustc 1.97   19/19 green
path2-two-shadow-recovery   rustc 1.97   30/30 green
```

### GPT-5.6 Pro — audit and GitHub Actions

GPT-5.6 Pro audited N-Nest, both recovery crates, Q-PRISM, healthcare GNNs, BigPickle, trained GNNs,
Hookwall/Shannon, white rooms, cube mint, Dispatcher, HyperHermes, reductions, and algorithms.

GPT-authored Rust 1.97.0 Actions runs completed successfully:

```text
Path 1      run 29134408321   exact 19-test assertion PASS
Path 2      run 29134413119   exact 30-test assertion PASS
Q-PRISM 3D run 29134419389   all targets PASS
```

## Claim ledger

- `MEASURED`: planted N-Nest faults caught at every demonstrated level; Path-2 recovery and DBWH
  re-projection tests.
- `MEASURED_CLAUDE_FABLE5_THIRD_SEAT`: supplied Rust recovery results.
- `MEASURED_GPT_DIRECTED_GITHUB_ACTIONS`: successful independent Rust CI.
- `AUDITED_GPT_5_6_PRO`: complete cross-repository audit.
- `CANON`: inverse-map verification is the shared theorem.
- `BOUNDARY`: storage is the durable state tier, not a GPU arithmetic substitute.
- `UNVERIFIED`: one live free-agent farm transaction combining trained GNN, cross-host Path 2,
  N-Nest depth-N verification, and hardware-enforced single-use shares.
